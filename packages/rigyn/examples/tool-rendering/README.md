# Tool replacement and rendering

This deliberately replaces the built-in `read` tool with a harmless bounded demonstration and supplies call/result renderers from `rigyn/tui`. Install it only when testing replacement precedence; removal restores the host tool.
