# Context and compaction

Adds one bounded instruction at `before_agent_start`. `/example-context` reports the host context estimate; `/example-context compact` requests host-owned compaction with custom guidance.
