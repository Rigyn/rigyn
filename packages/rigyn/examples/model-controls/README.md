# Model controls

This example reads and changes host-owned model state.

```text
rigyn install ./packages/rigyn/examples/model-controls
```

`/example-model` reports the active provider, model, and thinking level. Pass a
thinking level to the command to validate it and request the change through
the host selection API.
