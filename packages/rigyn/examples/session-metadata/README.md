# Session metadata

`/example-session-metadata NAME [ENTRY_ID]` demonstrates host-owned session naming, append-only custom entries, optional entry labels, and a custom entry renderer. The extension never opens or rewrites the session file itself.
