# Input and permission guard

Demonstrates bounded input interception and a fail-closed `bash` tool guard. It handles `/example-ignore`, truncates oversized submitted text, and blocks three privileged command names before execution.
