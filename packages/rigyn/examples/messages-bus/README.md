# Messages and event bus

`/example-message TEXT` emits a process-local topic. Its listener adds a custom session message with a structural renderer. The topic subscription is explicitly disposed on generation replacement.
